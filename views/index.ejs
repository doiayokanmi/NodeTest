<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <title>Node Test</title>

    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }
        main {
            height: 100vh;
            padding: 50px;
        }
        
        form {
            display: flex;
            align-items: center;
            width: 80vw;
        }
    </style>
</head>
<body>
    <main>
        <div>
            <form action="/details" method="post" class="form-control">
                <input class="form-control mb-2" type="text" name="item" placeholder="Enter Item">
                <input class="form-control " type="number" name="amount" placeholder="Enter Amount">
                <button class="btn btn-success btn-lg w-100 mt-2">Submit</button>
            </form>

            <table class="table table-striped table-success my-3">
                <tr>
                    <th>Name</th>
                    <th>Amount</th>
                </tr>
                <% for( let index = 0; index < userInput.length; index++ ) { %>
                    <tr class="bg-dark">
                        <td><%= userInput[index].item %></td>
                        <td><%= userInput[index].amount %></td>
                    </tr>
                <% } %>

            </table>

        </div>
    </main>
</body>
</html>